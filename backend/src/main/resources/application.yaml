server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  datasource:
    url: jdbc:postgresql://10.147.17.180:5432/nt118?TimeZone=UTC
    username: hunn
    password: hunn
    hikari:
      data-source-properties:
        TimeZone: UTC

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    database-platform: org.hibernate.dialect.PostgreSQLDialect
